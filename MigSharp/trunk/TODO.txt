This file contains global TODOs for the next release that are not in the source code
====================================================================================

- Document Teradata restriction when dropping primary keys
  - if their are added when creating the table, cannot
  - you should add them when creating the table because of performance because TD does some partitioning on it
  - adding a pk retrospectively is merely adding a unique constraint
- Document custom bootstrapping and custom versioning
- Document in Feature Overview the example of GUIDs
- fix all FIXME and NotImplementedException

Nice-to-have/backlog:
- find out *all* data types that are supported for keys
- publish with NuGet?
- see Roadmap in GitHub Wiki